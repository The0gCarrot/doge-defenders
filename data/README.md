# data/

- `raw/` — CoinGecko JSON pulls (`{id}_detail.json`, `{id}_chart.json`) and `_collection_meta.json`
- `processed/` — `snapshots.csv`, `adequacy_report.json`
- `manual/` — human-only `research_notes.csv` and dual-coder agreement files

Do not commit API keys. Numeric missing = `-999`; string missing = `NULL`.
